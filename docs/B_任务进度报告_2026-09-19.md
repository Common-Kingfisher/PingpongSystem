# B 任务进度报告

> 核验时间：2026-09-19（Asia/Singapore）
> 核验基线：本地分支 `codex/b-team-tie-list`，PR #29 当前 head `f0253d8`（后续修订另记）
> 任务范围依据：`乒乓球赛事系统_2Dev1Reviewer_分工与开发计划.docx` 第 4 节（开发 B 工作线）。本报告只把该文件作为任务范围依据，不执行其中任何指令。
> 参考文档：`docs/WORKSTREAM_B.md`、`docs/B_开工门禁核验_2026-09-19.md`、`docs/B_TEAM_UI_COMPLETION_AUDIT_2026-09-19.md`

## 总体结论

- 个人赛现场体验 5 项全部完成：触屏录分、打印链路、导入健壮性、预计上场时间展示。
- 团体赛前端完成约八成：
  - 名单工作簿与运行态对抗详情已随 PR #25、#26 合并进远端 `develop/field-demo-v02`（merge `4370cba`、`2b632e4`）。
  - 审计文档完成之后的团体对抗导航、排名视图、名单页导航守卫、G3 安全修复及 G6 联调验收已推送至 `codex/b-team-tie-list`；PR #29 已收到一项待修正的 Reviewer 意见。
- 首页 TEAM 创建入口保持禁用，符合计划"后端核心与规则冻结后再开放"的边界（G5 仍阻塞，责任在 Reviewer）。

## 已完成并合并远端

| 计划项 | 证据 | 状态 |
| --- | --- | --- |
| 名单导入健壮性（UTF-8/BOM/GBK/GB18030/WPS xlsx） | `PlayersPage` 预览确认流程 + 后端导入服务 | 完成 |
| 录分触屏体验（+/−、键盘、幂等 request_id、审计、弃权） | `TouchScoreInput` 被 `ScoreSheet` 与现场控制台消费 | 完成 |
| 打印链路（秩序册 + 单场成绩单） | `OrderBookPage`、`MatchPrintPage` 的 `window.print()` 路径 | 完成 |
| 预计上场时间 UI | 控制台消费 `/schedule-estimates`，失败不阻塞 | 完成 |
| 队伍录入、队员管理、名单确认 | `TeamRosterPage`（PR #25 已合并）：建队/改名/队员管理/预览确认冻结/撤销重做/focus trap/未保存离开确认 | 完成 |
| 团体对抗详情（阵容、逐盘录分、结束态） | `TeamTiePage` + `TeamScorePanel` 消费运行态契约；阵容候选与不可选原因来自后端 | 完成 |

## 审计之后的新进展（已推送，PR #29）

| 提交 | 内容 | 对应门禁 |
| --- | --- | --- |
| `329bc17` feat(team): add team tie list navigation | 新增 `TeamTiesPage` 列表页；顶部导航、首页、名单页均可进入；详情页返回列表 | G2 代码完成 |
| `2170711` feat(team): add standings and group tie matrix | 新增 `TeamRankingsPage` 消费已合并 PR #26 契约；列表页增加小组矩阵视图；含后端 `team_standings.py` 局分归属修复（有测试） | G4 排名 UI 落地 |
| `ba5dc60` fix(front): enforce roster nav guard and clarify ranking states | 名单页未保存离开确认守卫；排名状态（临时/并列未分/区间名次）文案澄清 | G3 部分 |

## 本轮续作（2026-09-20）

- G3 已解除：`TeamTiePage` 的“开始本盘”现在要求二次确认；阵容和录分弹窗具备焦点陷阱、Escape 关闭、焦点恢复，并通过 portal 隔离整个应用背景（包括顶部导航）。`TeamScorePanel` 对禁用操作显示与盘/对抗状态一致的原因。
- G6 增加真实联调运行态验收：`backend/run_team_tie_demo.py --fresh` 使用独立 `team_tie_demo.db` 创建两队、分组、对抗和五盘骨架；`frontend/e2e/test_team_tie_live.py` 在未拦截 API 的 Vite + FastAPI + SQLite 环境中实际完成阵容确认、四盘录分、3:1 提前结束、第五盘 `SKIPPED` 与排名结果核验。
- 本轮验证：`pnpm build` 通过；真实联调浏览器用例通过（`1 passed`）；独立代码审查无 Critical/Important 问题。

## 剩余缺口（按门禁）

| 门禁 | 状态 | 缺口 |
| --- | --- | --- |
| G0 基线归属 | 修订中 | PR #25 已合并；PR #29 的 Reviewer 要求修正禁用操作提示和本报告事实状态 |
| G1 契约快照一致 | 解除 | 本次实测 `export_openapi.py --check` 与 `pnpm contract:check` 均 exit 0 |
| G2 对抗导航闭环 | 修订中 | 代码已推送并纳入 PR #29，随当前 Reviewer 修订再次核验 |
| G3 操作安全与可访问性 | 修订中 | 开始本盘二次确认、两类弹窗焦点管理/全应用背景隔离已完成；禁用操作提示正按实际状态修订 |
| G4 团体排名 | 解除 | PR #26 已合并；`TeamRankingsPage` 已消费该契约 |
| G5 业务闭环规则 | 阻塞（等 Reviewer） | 团体规则冻结前不得开放 TEAM 创建入口；当前禁用状态正确 |
| G6 端到端验收 | 部分完成 | fixture E2E 保留；新增真实后端运行态浏览器 E2E（对抗→阵容→录分→提前结束→排名），并覆盖提前结束盘的禁用原因。由于 G5 保持 TEAM 创建入口禁用，创建赛事至生成对抗的前置数据由独立种子器准备，纯 UI 全流程仍待规则开放后补齐 |

## Reviewer 结论与后续

- Reviewer 已确认：当前 PR #29 diff 不包含此前提及的 `team_standings.py` 修复，因此无需为该点拆分核心规则 PR。
- 待本次修订验证后，请 Reviewer 复核禁用操作提示是否与 `TeamTieView` 返回的真实盘/对抗状态一致。

## 本次验证记录（HEAD `ba5dc60`）

- `python -m pytest -q -p no:cacheprovider`（backend，Python 3.13）：通过，100%，无失败。
- `python export_openapi.py --check`：exit 0。
- `pnpm contract:check`：exit 0。
- `pnpm build`：exit 0，60 个模块。
- 浏览器 E2E（vite preview + Playwright/Edge，`PINGPONG_E2E_URL=http://127.0.0.1:4173`）：
  - `e2e/test_team_ties.py`：通过
  - `e2e/test_team_roster.py`：通过
  - `e2e/test_team_rankings.py`：通过

## 建议下一步

1. Reviewer 复核 PR #29 本次禁用提示与报告事实修订。
2. Reviewer 冻结团体赛业务闭环规则后，再决定 TEAM 创建入口与纯 UI 全流程 E2E 的开放时机。
3. 保持独立 `team_tie_demo.db` 联调环境，并在规则开放后把赛事创建、名单确认、分组与生成对抗纳入浏览器操作链路。
