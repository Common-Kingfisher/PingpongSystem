# 变更记录

本文件记录面向使用者和协作者的功能变化。每个功能 PR 必须更新“未发布”章节；发布时再归入版本和日期。

## 未发布

### 新增

- 比赛时间基础（A2）：`matches` 新增 `called_at`（最近一次安排上球台）、`started_at`（当前有效进行中比赛的开始时间）、`finished_at`（当前有效比赛产生结果的时间）；统一 UTC SQLite 时间戳（`YYYY-MM-DD HH:MM:SS`），只由仓储层 helper 写入。手工排台与自动排台都会写时间；「下球台」清空 `started_at` 并保留 `called_at`，重新安排时刷新；改分不改变任何时间。
- 真实比赛耗时与赛事典型耗时（A2）：只把"真实打完"的比赛计入样本（有开始与结束时间、结果类型 NORMAL），排除系统轮空、未排台直接录分以及弃权 / 未到 / 取消资格等异常结果；赛事典型耗时取中位数，样本不足（< 3 场）时使用按赛制集中定义的冷启动技术估算值（产品估算值，不是官方时长）。
- 预计上场时间（A2 后端 V1）：`GET /api/tournaments/{id}/schedule-estimates` 返回每场待赛比赛的 `estimated_start_at`、`estimated_wait_minutes`、`queue_ahead` 与 `estimate_basis`；模拟复用自动排台的同一套规则，进行中的比赛按剩余时间推进（不会让已打很久的比赛再占满一个完整周期），签位未定或超出模拟范围时返回 null。
- 旧库自动迁移：启动时为历史数据库补充 `called_at` / `started_at` / `finished_at` 三列，原有数据保留。
- 撤销淘汰签表：`POST /api/tournaments/{id}/knockout/undo` 在淘汰赛尚未开始时删除主签与名次排位并恢复到 `GROUP_STAGE`，从而可以修正小组赛比分后重新生成签表；选手、分组、小组比赛与小组比分全部保留。
- 赛事结构化导出：`GET /api/tournaments/{id}/export` 一次返回 `schema_version`、赛事规则、选手、参赛位、分组、球台、比赛、大比分与小分、人工裁定（含冻结排名快照）、比分写入审计账本，以及运行期推导的排名 / 冠军 / 名次；导出为只读操作。
- 淘汰赛交叉对阵按已冻结范围实现：2 组 × 每组前 2、4 组 × 每组前 2（A1-D2、C1-B2、B1-C2、D1-A2）以及偶数组 × 每组前 2 的"首尾交叉"原则；同组两人与 1/2 号种子分处不同半区。需要轮空时的具体轮空落位、奇数组、每组出线人数 != 2 仍未正式冻结，属确定性兼容实现。
- 正式（LIVE）赛事开放 Excel / CSV 导入：与演示赛事一样支持预览 → 确认 → 名单出现，演示选手生成仍限 DEMO。
- 种子单一数据源：修改选手种子后同步对应 Entry 种子，分组与签表按同一份种子执行；新增「按积分生成种子」（单打，积分降序、同分按选手编号，可手工再调整）。
- 创建赛事可选择比赛局制（三局两胜 / 五局三胜 / 七局四胜）与每局目标分；控制台、录分弹窗、秩序册文案统一按赛事配置动态显示。
- 球台调度改为"亲和 + 公平"：硬约束 → 组台亲和 → 组间进度公平 → 连续上场惩罚 → 稳定顺序；控制台展示服务端给出的每台建议。

### 修复

- 自动排台不再让落后的组饥饿：球台少于小组数时各组轮转推进，球台多于小组数时多余球台优先服务落后组。
- Demo「模拟完成剩余小组赛」按赛事局制生成合法比分，五局三胜 / 七局四胜不再返回 500。
- 分批调度优先避免刚完成比赛的运动员立即再次上场；该规则为软性排序，在没有其它可执行比赛时仍允许继续安排。

### 新增（此前批次）

- 增加 120 人规模验收名单（CSV/XLSX）和 24 组×5 人的全流程容量回归测试。
- 赛事运行模式：`LIVE` 正式赛事与 `DEMO` 演示赛事。
- 比分请求唯一编号和服务端幂等处理。
- 主裁判可在自动规则和已录小分仍不能确定晋级线时，人工选择准确数量的晋级参赛位。
- 人工裁定记录选择结果、理由、操作者、时间和排名依据快照，并提供历史查询与撤销接口。

### 安全

- 正式赛事由后端禁止生成演示选手和随机完成小组赛。
- 删除正式赛事必须准确提交完整赛事名称。
- 淘汰赛已经开始（存在进行中比赛或已录结果）时拒绝撤销签表并返回 409，不静默删除真实比赛结果；系统轮空（一方为空）不算真实结果，可随签表一并撤销。
- 删除赛事前推荐先调用赛事导出接口留存结构化备份；删除接口在冲突提示中给出该建议。
- 前端在网络中断时使用相同请求编号重试比分提交，避免重复写入。
- 比分、小比分或小组出线人数变化时，当前人工裁定自动失效，避免旧晋级名单继续生效。

### 文档

- 建立随功能 PR 同步维护 README、操作手册、交接、路线图、OpenAPI 和变更记录的约定。
